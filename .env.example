# CUMPAVIO environment variables
#
# Copy this file to .env.local for local development when environment
# variables are introduced.
#
# Never commit secrets.
#
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
# Server-only secrets must never use the NEXT_PUBLIC_ prefix.

# No environment variables are required during Stage 1.